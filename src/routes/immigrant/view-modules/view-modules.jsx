import { makeStyles } from "@material-ui/core";
import React from "react";

import { ViewModules as ViewModulesComponent } from "../../../components";

export const ViewModules = ({ history }) => {
  return <ViewModulesComponent history={history} />;
};
