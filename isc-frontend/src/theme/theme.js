import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#e82727"
    },
    secondary: {
      main: "#e82727"
    }
  },
  status: {
    danger: "orange"
  }
});

export default theme;
