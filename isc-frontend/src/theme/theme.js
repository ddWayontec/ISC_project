import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#c62828',
    },
    secondary: {
      main: '#c62828',
    },
  },
  status: {
    danger: 'orange',
  },
});

export default theme;
